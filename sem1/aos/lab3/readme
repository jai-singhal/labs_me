
    // for (int i = 0; i < 2; i++) {
    //     child_id = fork();

    //     if (child_id == 0) {
    //             // child process
    //             funcion();
    //             exit(0);
    //     }

    //     process_id[i] = child_id;
    // }

    // for (int i = 0; i < 2; i++)
    //     waitpid(process_id[i], &status, 0);
  